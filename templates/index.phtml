<html doctype="html">
    <head>
        <title>{{$title}}</title>
        <meta name="title" content="{{$title}}"/>
        {{$headfiles}}
    </head>
    <body>
        {{$header}}
        {{$menu}}
        <!-- контент сюда попадает из файла , которий засетан в текущем контролере - например $template->setFile('templates/main.phtml') -->
       <div class="content"> {{$content}} </div>
        {{$footer}}
    </body>
</html>