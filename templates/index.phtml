<html doctype="html">
    <head>
        <title>{{$title}}</title>
        <meta name="title" content="{{$title}}"/>
        {{$headfiles}}
    </head>
    <body>
        {{$content}}
    </body>
</html>